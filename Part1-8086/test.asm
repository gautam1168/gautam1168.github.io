bits 16

ADD CX, AX
ADD AL, CL
ADD BH, CH
ADD AL, 0x10
ADD AX, 0xfaf
PUSH ES
POP ES
ADD CX, AX
ADD AL, CL
ADD BH, CH
ADD AL, 0x10
ADD AX, 0xfaf
PUSH CS

ADC CX, AX
ADC AL, CL
ADC BH, CH
ADC AL, 0x10
ADC AX, 0xfaf
PUSH SS
POP SS
SBB CX, AX
SBB AL, CL
SBB BH, CH
SBB AL, 0x10
SBB AX, 0xfaf
PUSH DS
POP DS

AND CX, AX
AND AL, CL
AND BH, CH
AND AL, 0x10
AND AX, 0xfaf
DAA
SUB CX, AX
SUB AL, CL
SUB BH, CH
SUB AL, 0x10
SUB AX, 0xfaf
DAS

XOR CX, AX
XOR AL, CL
XOR BH, CH
XOR AL, 0x10
XOR AX, 0xfaf
AAA
CMP CX, AX
CMP AL, CL
CMP BH, CH
CMP AL, 0x10
CMP AX, 0xfaf
AAS

INC AX
INC CX
INC DX
INC BX
INC SP
INC BP
INC SI
INC DI
DEC AX
DEC CX
DEC DX
DEC BX
DEC SP
DEC BP
DEC SI
DEC DI

PUSH AX
PUSH CX
PUSH DX
PUSH BX
PUSH SP
PUSH BP
PUSH SI
PUSH DI
POP AX
POP CX
POP DX
POP BX
POP SP
POP BP
POP SI
POP DI

NOP
XCHG AX, CX
XCHG AX, DX
XCHG AX, BX
XCHG AX, SP
XCHG AX, BP
XCHG AX, SI
XCHG AX, DI
CBW
CWD
WAIT
PUSHF
POPF
SAHF
LAHF

MOV AX, BX
